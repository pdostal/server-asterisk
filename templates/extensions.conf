[globals]

[general]

[default]
exten => s,1,Answer()
exten => s,n,Wait(1)
exten => s,n,Playback(tt-weasels)
exten => s,n,Hangup()

[public]

[incoming]

[outgoing]
exten => _1X.,1,Set(CALLERID(num)=00420245009808)
exten => _1X.,n,Set(CALLERID(name)=Pavel Dostál)
exten => _1X.,n,Dial(SIP/haloo/${EXTEN:1})

exten => _2X.,1,Set(CALLERID(num)=00420910117573)
exten => _2X.,n,Set(CALLERID(name)=Pavel Dostál)
exten => _2X.,n,Dial(SIP/odorik/${EXTEN:1})

exten => _3X.,1,Set(CALLERID(num)=00447989316698)
exten => _3X.,n,Set(CALLERID(name)=Pavel Dostál)
exten => _3X.,n,Dial(SIP/sipgate/${EXTEN:1})

[internal]
exten => pavel,1,Dial(SIP/pavel,,)

[phones]
include => internal
include => outgoing
